# Architecture Decision Records

This directory records the significant technical, feature, and architecture decisions made while building Fabula — the reasoning behind them, the alternatives considered, and their consequences. `docs/PRD.md` and `docs/use-cases.md` define *what* the product does; these records explain *why* it's built the way it is.

Format (lightweight [Nygard-style](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions)):

- **Status** — accepted, superseded, etc.
- **Context** — the problem or constraint that forced a decision.
- **Decision** — what was chosen.
- **Consequences** — what that choice makes easy, hard, or worth revisiting later.

New ADRs are numbered sequentially and are not edited after acceptance — a changed decision gets a new ADR that supersedes the old one, so the record of *why* something changed is preserved rather than overwritten. However, editorial-only improvements to prose, grammar, and readability are permitted without requiring a new ADR.

## Index

1. [Model-agnostic provider interface](0001-model-agnostic-provider-interface.md) — the `LLMProvider` abstraction and registry pattern.
2. [Provider and model selection](0002-provider-model-selection.md) — why Claude Sonnet 5, GPT-5 mini, and Llama 3.3 70B.
3. [Streaming wire protocol](0003-streaming-wire-protocol.md) — how `/api/generate` streams prose and carries invented-metadata out of band.
4. [Strict turn-taking policy](0004-strict-turn-taking-policy.md) — one paragraph per turn, enforced server-side.
5. [Context window management](0005-context-window-management.md) — the anchor-plus-recency rolling window.
6. [Content safety defaults](0006-content-safety-defaults.md) — age-appropriate output as a prompting default, not a toggle.
7. [Client state architecture](0007-client-state-architecture.md) — cross-route story state without a database or storage APIs.
8. [Merged actions and narrative-arc pacing](0008-merged-actions-and-narrative-pacing.md) — one button per screen, the stale-closure fix, and soft-target climax/resolution steering.
9. [Accounts and persistence architecture](0009-accounts-and-persistence-architecture.md) — Neon + Drizzle, Auth.js v5 with JWT sessions, and the write-through-mirror persistence model.
10. [Shared story feed and safety](0010-shared-story-feed-and-safety.md) — logged-in-only feed, the unmoderated-content risk, disclaimer + report mitigation, and explicit non-goals.
11. [Security hardening after the v2 review](0011-security-hardening-post-review.md) — origin-resolved redirect validation, uninformative registration responses, and the shared server-side input trust boundary.
12. [Pinning npm in CI](0012-ci-npm-version-pinning.md) — why the `EBADPLATFORM`/AIX build failure was an npm major-version mismatch, not a platform-specific lockfile.
13. [Concurrency-safe paragraph positioning](0013-concurrency-safe-paragraph-positioning.md) — the unique constraint, content-based reconciliation, and why atomicity is expressed as a single statement. Its write mechanism is amended by 16.
14. [Test infrastructure and driver parity](0014-test-infrastructure-and-driver-parity.md) — the three Vitest projects, injection over mocking, the `AppDatabase` divergence guard, and deterministic race tests.
15. [Postgres-backed rate limiting](0015-postgres-backed-rate-limiting.md) — a token bucket in one statement, why in-memory limiting is broken on serverless, and failing closed.
16. [Asserted paragraph positions](0016-asserted-paragraph-positions.md) — why re-deriving `max(position)` at write time defeated 13's constraint, and writing the validated position instead.
17. [Hot-path indexes and the NULLS ordering trap](0017-hot-path-indexes.md) — the five indexes, verifying plans rather than timings, and the `DESC NULLS LAST` mismatch that made the library index unusable for ordering.
18. [LLM evaluation harness](0018-llm-evaluation-harness.md) — the three layers (prompt snapshots, fixture replay through real adapters, nightly live drift), the pinned direct-SDK judge, and hard-failure staleness.
19. [End-to-end test strategy](0019-end-to-end-test-strategy.md) — the mock provider's remote-control channel, the Neon HTTP proxy driver-parity harness, per-test truncation and the rate-limit-bucket trap, and named gaps (Google OAuth, cross-browser, provider timeouts).
20. [CI-only expect timeout headroom, and how not to diagnose e2e flakiness](0020-e2e-ci-timeout-headroom.md) — why a plausible-looking connection-pool-race theory was wrong, how interleaved webServer log output misled that diagnosis, and the soak-test discipline adopted to stop it happening again.
21. [guest-write.spec.ts flake: current status is mitigated, not resolved](0021-guest-write-flake-status.md) — the 15s timeout failed a third time with new evidence (stuck, then completes all at once), a diagnostic-logging attempt that a lucky pass made inconclusive, and why that logging was removed rather than left dangling.
22. [Observability, structured logging, and cost accounting](0022-observability-and-cost-accounting.md) — OTel traces and structured logs over a vendor SDK, usage riding the generator's return value, the structural (not reviewed) privacy allowlist, why `/api/health` fails open on its own rate limiter, and the `generation_event` table's Postgres-vs-NoSQL reasoning.
23. [Provider timeouts and Writer-mediated failover](0023-provider-timeouts-and-writer-mediated-failover.md) — idle-based first-chunk/stream timeouts on one AbortController, why fast failures retry once but timeouts and mid-stream stalls never do, and why failover asks rather than silently switches.
24. [Security headers and supply-chain automation](0024-security-headers-and-supply-chain.md) — nonce-based CSP in `proxy.ts` composed with the existing auth redirect, report-only-then-enforce verification, the directives loosened from strict defaults and why, and Dependabot/CodeQL/`npm audit` automation.
25. [guest-write.spec.ts flake: root cause found and fixed](0025-guest-write-flake-root-cause-and-fix.md) — the aborted-fetch-vs-navigation race found via live CI trace inspection, why only the guest first-paragraph path hit it, and awaiting generation kickoff before navigating. Superseded by 26.
26. [guest-write.spec.ts flake: the real fix, and why the first two weren't it](0026-guest-write-flake-actual-fix.md) — a local `--repeat-each` repro loop (contradicting 20/21's "never reproduces locally"), `onChunk`/`onDone`/`onError` instrumentation proving the app never observes a failure, and the actual fix (real mock `delayMs` margin for an intermediate-paint assertion).
27. [CI quality gates: responsive, accessibility, bundle budget, typecheck](0027-ci-quality-gates.md) — absorbing the responsive script into Playwright, axe at serious/critical only, why the bundle-budget script reads Turbopack's actual per-route manifest instead of the webpack-era files originally planned, and the measured baseline.
28. [Nightly eval-drift debounce and wider tolerance](0028-nightly-eval-drift-debounce.md) — diagnosing 10-for-10 nightly failures as single-sample LLM-judge noise (not synced three-vendor drift), widening `nightlyDriftTolerance`, and requiring two consecutive nights before a score-based failure fails the job. Amends 18.
29. [Redesign tokens: measured contrast over the handoff's claimed numbers, and locked theme namespaces](0029-redesign-tokens-and-contrast.md) — why `--muted` sets at 0.66 alpha instead of the handoff's 0.55 (3.62:1 measured, not the 4.9:1 claimed), why inline accent links use `--accent-text`, and why Tailwind's stock color/font/radius/shadow namespaces are cleared rather than left alongside the design-system tokens.
30. [Redesign atoms: outlined buttons, underline fields, chips, and the two-voices mark](0030-redesign-atoms-buttons-fields-chips.md) — atoms as a CSS component layer (and why base styles had to be layered too), `--accent-text` on button labels and other sub-AA alpha text, the 44px chip wrapper, and regenerating `favicon.ico` instead of deleting it.
31. [Story canvas restructure: gutter attribution, arc rail, and what left the canvas](0031-story-canvas-restructure.md) — the target-length slider leaves the canvas (and the resume test moves with it), Save/Share as links rather than new state, CSP-safe SVG for the rail and progress bar, the dimmed-composer contrast exception, and the `Add & continue` copy change.
32. [Start flow as a five-step slide](0032-start-flow-stepper.md) — inert off-stage panels with focus handoff, the slide as static classes for CSP, the native range kept under the tick ruler, e2e walking the rail, and the phone footer wrap.
33. [Shared story as a printed piece, and the auth screens](0033-shared-story-and-auth-screens.md) — the theme as the masthead title instead of a new column, the model name from the registry, an unprefilled "Start one like this", and the `::first-letter` drop cap.
34. [Capacity benchmark harness](0034-capacity-benchmark-harness.md) — a bespoke `tsx` script over k6/artillery, why every virtual writer shares one authenticated identity, counting DB round trips with a Proxy (and the get-vs-call over-counting bug it caught), the `%5F%5Fbench` private-folder gotcha, and why round-trip counting and concurrency measurement run as separate, non-overlapping phases.
35. [Redis as a non-authoritative tier](0035-redis-as-a-non-authoritative-tier.md) — `@upstash/redis` over the wire protocol, the never-authoritative rule and its per-mechanism fallback table, why the Postgres token bucket is kept rather than replaced, and why a blanket test-suite `FLUSHALL` is safe for the single-worker E2E suite but actively unsafe for the parallel-worker Vitest `db` project.
36. [Admission control and spend governance](0036-admission-control-and-spend-governance.md) — concurrency leases with a TTL and one release site inside the existing `finish()` teardown, the global-cap-fails-closed/per-identity-fails-open asymmetry, reconciling from `generation_event` on a cold key, the shared guest budget's honest limits, the trusted-proxy `x-forwarded-for` fix, and the new rate-limit policies for the previously-unguarded story/feed/report routes.
